<?xml version="1.0" encoding="UTF-8" ?>
<Shell xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
       xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
       xmlns:views="clr-namespace:GroupProject.View"
       x:Class="GroupProject.View.AppShell"
       Shell.FlyoutBehavior="Disabled">

	<ShellContent Route="Login" ContentTemplate="{DataTemplate views:LoginPage}" />
	<ShellContent Route="Register" ContentTemplate="{DataTemplate views:RegisterPage}" />
	<ShellContent Route="Dashboard" ContentTemplate="{DataTemplate views:DashboardPage}" />
	<ShellContent Title="Account" Route="AccountPage" ContentTemplate="{DataTemplate views:AccountPage}" />
       <ShellContent Route="Test" ContentTemplate="{DataTemplate views:MainPage}"/>
       <ShellContent Route="Challenges" ContentTemplate="{DataTemplate views:ChallengesPage}"/>

    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate local:RegisterPage}"
        Route="RegisterPage" />

    <ShellContent
        Title="Dashboard"
        ContentTemplate="{DataTemplate local:DashboardPage}"
        Route="DashboardPage" />

    <ShellContent
        Title="Account"
        ContentTemplate="{DataTemplate local:AccountPage}"
        Route="AccountPage" />

    <ShellContent
        Title="Sandbox"
        ContentTemplate="{DataTemplate local:PuzzlePage}"
        Route="PuzzlePage" />

	<ShellContent
		Title="Learner"
		ContentTemplate="{DataTemplate local:LearnerPage}"
		Route="LearnerPage" />

    <ShellContent
        Title="Trending"
        ContentTemplate="{DataTemplate local:TrendingPage}"
        Route="TrendingPage" />

    <!-- <ShellContent
        Title="MichaelsPage"
        ContentTemplate="{DataTemplate local:MichaelsPage}"
        Route="MichaelsPage" /> -->
</Shell>

